<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>الطلبات</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet" />
</head>
<body class="container mt-5">
  <h1>📦 الطلبات الواردة</h1>

  <% if (orders.length === 0) { %>
    <div class="alert alert-info text-center">لا توجد طلبات حاليًا.</div>
  <% } else { %>
    <ul class="list-group">
      <% orders.forEach(order => { %>
        <li class="list-group-item">
          <strong>🔸 <%= order.productName %></strong><br/>
          <small>من: <strong><%= order.buyerUsername %></strong></small><br/>
          <small>الوصف: <%= order.productDescription %></small><br/>
          <small>الميزانية: <%= order.proposedBudget %> ريال</small><br/>
          <small>الحالة: <span class="badge bg-secondary"><%= order.status %></span></small><br/>
          <small>بتاريخ: <%= new Date(order.createdAt).toLocaleDateString('ar-EG') %></small>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
